language: python
notifications:
  email: false
python:
- "3.4"
install:
- if [[ $TRAVIS_PYTHON_VERSION == 2.6 ]]; then pip install ordereddict; fi
- pip install mock moto
- python setup.py install
script:
- python -W ignore smart_open/tests/test_smart_open.py
